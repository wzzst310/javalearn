server:
  port: 8024
spring:
  application:
    name: cc-provider-feign-dynamictarget
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true